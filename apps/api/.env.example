# Environment
NODE_ENV=development

# Server
PORT=4000
HOST=0.0.0.0

# CORS
CORS_ORIGIN=http://localhost:3000

# AI Provider Keys (OPTIONAL — for local development)
# In production, users provide their own keys via the UI.
# DEEPSEEK_API_KEY=
# OPENROUTER_API_KEY=
# GROQ_API_KEY=